nextflow_workflow {

    name "Test Workflow ANNOTATE_METHYLATION"
    script "../main.nf"
    config "./nextflow.config"
    workflow "ANNOTATE_METHYLATION"

    test("1 sample") {
        when {
            workflow {
                """
                input[0] = channel.of( [
                    [ id:'test', family_id:'FAM'],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                ]
                )
                input[1] = channel.of(
                    [
                        [ id:'test2'],
                    file(params.pipelines_testdata_base_path + 'reference/methbat_map.tsv', checkIfExists: true)
                    ]
                )
                """
            }
        }

         then {
            assert workflow.success
            assertAll(
                { assert snapshot(workflow.out).match() }
            )
        }
    }

    test("2 samples") {
        when {
            workflow {
                """
                input[0] = channel.of(
                    [
                        [ id:'test', family_id:'FAM' ],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                    ],
                    [
                        [ id:'test2', family_id:'FAM' ],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                    ]
                )
                input[1] = channel.of(
                    [
                        [ id:'test2'],
                    file(params.pipelines_testdata_base_path + 'reference/methbat_map.tsv', checkIfExists: true)
                    ]
                )
                """
            }
        }

         then {
            assert workflow.success
            assertAll(
                { assert snapshot(workflow.out).match() }
            )
        }
    }

    test("1 sample - stub") {

        options "-stub"

        when {
            workflow {
                """
                input[0] =  channel.of([
                    [ id:'test', family_id:'FAM'],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                ]
                )
                input[1] = channel.of(
                    [
                        [ id:'test2'],
                    file(params.pipelines_testdata_base_path + 'reference/methbat_map.tsv', checkIfExists: true)
                    ]
                )
                """
            }
        }

        then {
            assert workflow.success
            assertAll(
                { assert snapshot(workflow.out).match() }
            )
        }
    }

    test("2 samples - stub") {

        options "-stub"

        when {
            workflow {
                """
                input[0] = channel.of(
                    [
                        [ id:'test', family_id:'FAM' ],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                    ],
                    [
                        [ id:'test2', family_id:'FAM' ],
                    file(params.pipelines_testdata_base_path + 'testdata/charmedswan_methbat_profile.tsv', checkIfExists: true)
                    ]
                )
                input[1] = channel.of(
                    [
                        [ id:'test2'],
                    file(params.pipelines_testdata_base_path + 'reference/methbat_map.tsv', checkIfExists: true)
                    ]
                )
                """
            }
        }

         then {
            assert workflow.success
            assertAll(
                { assert snapshot(workflow.out).match() }
            )
        }
    }
}
