process {
    withName: 'HIFIASM' {
        ext.args = { [
            '--dual-scaf',
            '--telo-m CCCTAA'
        ].join(' ') }
    }

    withName: 'YAK_COUNT' {
        ext.args = '-k31 -b0'
        ext.prefix = { "${meta.id}_yak" }
    }

    withName: 'GFASTATS' {
        ext.args = '--discover-paths'
        ext.prefix = { "${meta.id}_haplotype_${meta.haplotype}" }
    }
}
