# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: "sniffles1_sv"
description: Call structural variants using Sniffles (v1)
keywords:
  - sv calling
  - sniffles1
  - long-read sequencing
components:
  - sniffles1
input:
  - ch_bam_bai:
      type: file
      description: |
        Channel containing sample information (meta), BAM and BAI files
        Structure: [ val(meta), path(bam), path(bai) ]
      pattern: "*.{bam,cram}"
output:
  - vcf:
      type: file
      description: |
        Channel containing sample information (meta, with sv_caller, needs_reheader, skip_vep_prep stamped)
        and raw VCF file
        Structure: [ val(meta), path(vcf) ]
      pattern: "*.vcf.gz"
  - versions:
      type: file
      description: File containing software versions
      pattern: "versions.yml"
authors:
  - "@aksenia"
maintainers:
  - "@aksenia"
