process {

    withName: '.*:QC_PHASING:.*' {
        publishDir = [
            enabled: false,
        ]
    }

    withName: '.*:QC_PHASING:BCFTOOLS_CONCAT' {
        ext.prefix = { "${meta.id}_phased"}
        ext.args = [
            '--allow-overlaps',
            '--output-type z',
            '--write-index=tbi'
        ].join(' ')
    }

    withName: '.*:QC_PHASING:CRAMINO' {
        ext.args = {
            [
                '--karyotype',
                '--phased',
                "--arrow ${meta.id}_cramino_aligned_phased.arrow",
                '--ubam'
            ].join(' ')
        }
        ext.prefix = { "${meta.id}_cramino_aligned_phased" }
        publishDir = [
            path: { "${params.outdir}/qc/cramino/phased/${meta.id}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    withName: '.*:QC_PHASING:WHATSHAP_STATS' {
        ext.prefix = { "${meta.id}_stats" }
        ext.args = { "--sample \"${meta.id}\"" }
        publishDir = [
            path: { "${params.outdir}/qc/phasing_stats/${meta.id}" },
            mode: params.publish_dir_mode,
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }



}
