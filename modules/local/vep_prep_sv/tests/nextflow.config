process {
    withName: SNIFFLES {
        ext.prefix = { "${meta.id}_sniffles" }
    }
    withName: VEP_PREP_SV {
        ext.prefix = { "${meta.id}_vep_prep" }
        ext.args   = params.caller ? "--caller ${params.caller}" : ''
    }
}
